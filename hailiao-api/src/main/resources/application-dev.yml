# =============================================================================
# 开发环境（profile = dev）— API
# =============================================================================
# 连接信息默认写在本文件；仅需启动前设置 APP_ENV=dev（或不设置，默认即 dev）

server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hailiao?useUnicode=true&characterEncoding=utf-8&characterSetResults=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: carmi2023_

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 6000ms

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.hailiao: DEBUG
    org.springframework.security: DEBUG

springfox:
  documentation:
    swagger-ui:
      enabled: true

# 本地开发 OSS：请替换为你的阿里云测试凭据（勿提交真实 Key 到公开仓库）
oss:
  endpoint: https://oss-cn-hangzhou.aliyuncs.com
  access-key-id: LTAI5tBVCPzmq744f12MvvEL
  access-key-secret: Ie5iVGPKRsXCqEigGXrZGXPyMCYvmH
  bucket-name: hailiao-im
  domain: https://hailiao-im.oss-cn-hangzhou.aliyuncs.com
  prefix: hailiao
