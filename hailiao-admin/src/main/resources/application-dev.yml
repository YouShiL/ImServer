# =============================================================================
# 开发环境（profile = dev）— 管理后台
# =============================================================================

server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hailiao?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: carmi2023_

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 6000ms

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.hailiao: DEBUG
    org.springframework.security: DEBUG
