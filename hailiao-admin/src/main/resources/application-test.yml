# =============================================================================
# 测试环境（profile = test）— 管理后台
# =============================================================================

server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hailiao_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: carmi2023_

  redis:
    host: localhost
    port: 6379
    database: 1
    timeout: 6000ms

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.hailiao: INFO
    org.springframework.security: INFO
